# 백엔드 연동 가이드

## 전체 흐름

```
[앱] 사진 + 설문
      │
      ▼
[백엔드] ──①──▶ 피부 분석 API :8001  /analyze
      │◀── {cnn_result, llm_result, survey} ──┘
      │
      ├──②──▶ 추천 API :8000  /recommend
      │◀── {헤드라인, 구성:[토너,로션,크림], 총액} ──┘
      ▼
    [앱]
```

**①의 응답을 그대로 ②에 POST하면 됩니다.** 변환 코드 불필요.

---

## ① 피부 분석 API

### `POST /analyze`  (multipart/form-data)

| 필드 | 타입 | 설명 |
|---|---|---|
| `image` | file | 얼굴 사진 (jpeg/png/webp, 최대 10MB) |
| `answers` | string | 설문 응답 JSON. 한글 라벨 그대로 |
| `top_k` | int | 기본 3 |

```bash
curl -X POST http://localhost:8001/analyze \
  -F "image=@face.jpg" \
  -F 'answers={"skin_type":"지성","main_concern":["여드름/뾰루지","블랙헤드/모공"]}'
```

**응답**
```json
{
  "cnn_result": {
    "predicted_label": "acne_rosacea",
    "confidence": 0.9891,
    "top_k": [{"label":"acne_rosacea","confidence":0.9891}, ...]
  },
  "llm_result": {
    "summary": "...", "likely_conditions": [...],
    "care_recommendations": [...], "need_professional_care": true,
    "disclaimer": "..."
  },
  "survey": { "skin_type": 2, "concerns": [1,2] }
}
```

### `GET /survey`
설문 문항 스키마. **프론트가 화면 그릴 때 사용.**
문항 텍스트를 하드코딩하지 말고 여기서 받아가세요.

### `GET /health`
`model_loaded`로 모델 로딩 여부 확인.
**최초 요청 때 모델을 로드하므로 첫 호출만 느립니다(5~15초).**
기동 직후 `/analyze`를 한 번 미리 때려두면 좋습니다.

---

## ② 추천 API

### `POST /recommend`  (application/json)

①의 응답 **그대로** + 예산(선택)

```js
const analysis = await fetch("http://localhost:8001/analyze", {
  method: "POST", body: formData
}).then(r => r.json());

const routine = await fetch("http://localhost:8000/recommend", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ ...analysis, budget_total: 90000 })
}).then(r => r.json());
```

응답 필드는 `백엔드_인계문서.md` 참고.

---

## 실행

```bash
docker compose up -d
```

`model/.env`에 LLM API 키가 필요합니다 (`.env.example` 참고).

Docker 없이 띄우려면:
```bash
cd model && uvicorn server:app --port 8001 &
uvicorn main:app --port 8000 &
```

---

## 주의사항

| 항목 | 내용 |
|---|---|
| **타임아웃** | ①이 CNN+LLM이라 3~10초 걸립니다. 백엔드 타임아웃 **30초** 권장 |
| **순차 호출** | ①의 결과가 ②의 입력이라 병렬 불가 |
| **이미지 크기** | 10MB 제한. 앱에서 리사이즈 후 업로드 권장 |
| **의료 권고** | ②의 `의료상담권고: true`면 **제품보다 병원 안내 먼저** |
| **첫 요청** | 모델 로딩으로 느림. 헬스체크로 워밍업 |
