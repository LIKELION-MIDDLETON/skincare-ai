# 추천 API 인계 문서 (v5)

## 한 줄 요약
**피부분석 결과(CNN+LLM) + 설문 응답을 그대로 POST하면 → 토너·로션·크림 3개가 나옵니다.**
백엔드는 변환 없이 받은 JSON을 그대로 전달하면 됩니다.

---

## 1. 실행
```bash
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8000
```
Docker: `docker build -t skincare-api . && docker run -p 8000:8000 skincare-api`
실행 후 `http://localhost:8000/docs` 에서 직접 테스트 가능.

---

## 2. INPUT — 피부분석 결과를 그대로

```json
{
  "cnn_result": {
    "predicted_label": "acne_rosacea",
    "confidence": 0.9891,
    "top_k": [
      {"label":"acne_rosacea","confidence":0.9891},
      {"label":"psoriasis_lichen_planus","confidence":0.0052},
      {"label":"pigmentation_disorder","confidence":0.0022}
    ]
  },
  "llm_result": {
    "summary": "...",
    "care_recommendations": ["...","..."],
    "need_professional_care": true,
    "disclaimer": "..."
  },
  "survey": {
    "skin_type": 1,
    "concerns": [2,4,5],
    "duration": 5,
    "areas": [4,3],
    "irritation": 2,
    "diagnosed": 2
  },
  "budget_total": 90000
}
```

| 블록 | 필수 | 설명 |
|---|---|---|
| `cnn_result` | **O** | 진단 모델 출력 그대로. `top_k`가 3개뿐이어도 나머지는 자동 보정 |
| `llm_result` | X | `need_professional_care`가 의료 권고에 반영됨. 나머지는 그대로 응답에 실려 나감 |
| `survey` | X | 설문 번호 그대로. 있으면 추천 품질이 크게 올라감 |
| `budget_total` | X | 3개 합계 예산(원) |

### 설문 코드표
| 문항 | 코드 |
|---|---|
| `skin_type` | 1건성 2지성 3복합성 4수부지 5민감성 6모름 |
| `concerns` (복수) | 1여드름 2블랙헤드/모공 3홍조 4건조/각질 5색소침착 6주름/탄력 7가려움/따가움 8없음 |
| `duration` | 1해당없음 2~1주 3 1~4주 4 1~3개월 5 3개월이상 |
| `areas` (복수) | 1이마 2코 3볼 4턱 5눈가 6전체 7없음 |
| `irritation` | 1전혀 2가끔 3자주 |
| `diagnosed` | 1없음 2아토피 3여드름중증 4지루성 5건선 6기타 |

**설문이 추천에 미치는 영향**
- `skin_type` → 보습/피지조절 가중치. `수부지`는 보습+피지조절 동시 적용
- `concerns` → 해당 효능축 부스트 (예: 5색소침착 → 미백 +2.5)
- `irritation` 3 또는 `skin_type` 5(민감성) 또는 `diagnosed` 2/5 → **무향 제품만 필터링**
- `duration` 5(3개월 이상) → 의료 권고 강화

---

## 3. OUTPUT

```json
{
  "헤드라인": "당신은 여드름·주사성 피부입니다",
  "요약": "피지·모공 관리와 진정을 함께. 유분 많은 제형은 피하세요.",
  "신뢰도": 0.989,
  "트리아지": "미용케어",
  "의료상담권고": true,
  "의료권고사유": ["AI 분석 소견","진단 이력(아토피피부염)","증상 3개월 이상 지속"],
  "반영된설문": {"피부타입":"건성","고민":["블랙헤드/모공","건조함/각질","색소침착/기미"],"무향필터":"강제"},
  "분석요약": "(llm_result.summary 그대로)",
  "생활수칙": ["(llm_result.care_recommendations 그대로)"],
  "고지사항": "(llm_result.disclaimer 그대로)",
  "총액": 65100,
  "구성": [
    {
      "순서": 1, "슬롯": "토너",
      "goods_no": "A000000182686",
      "brand": "에스트라", "name": "에스트라 테라크네365 하이드로 액티브 토너 200ml",
      "가격": 25500,
      "적합도": {"지성적합":24.0,"저자극":68.0,"진정효과":65.0},
      "적합도출처": "실측",
      "고시": "미백:나이아신아마이드, 여드름:살리실릭애씨드",
      "무향": true, "코메도": 0
    }
  ]
}
```

### 화면 매핑
| 위치 | 필드 |
|---|---|
| 결과 상단 제목 | `헤드라인` |
| 부제 | `요약` |
| 제품 카드 3장 | `구성[]` — `순서`가 바르는 순서 (토너→로션→크림) |
| 카드 내 추천 이유 | `적합도` |
| 신뢰 배지 | `적합도출처` (`실측`=리뷰 기반, `예측`=모델 추정) |
| 구매 링크 | `https://www.oliveyoung.co.kr/store/goods/getGoodsDetail.do?goodsNo={goods_no}` |
| 장바구니 합계 | `총액` |
| 생활 수칙 섹션 | `생활수칙` |
| 하단 고지 | `고지사항` |

---

## 4. 백엔드가 반드시 처리할 것

| 상황 | 처리 |
|---|---|
| `의료상담권고 == true` | **제품보다 병원 안내를 먼저** 노출. `의료권고사유` 배열을 근거로 표시 |
| `신뢰도 < 0.5` | "참고용" 배지 |
| HTTP 400 | 진단 모델 출력 형식 오류 |
| `적합도출처 == "실측"` | "리뷰 기반" 배지 → 신뢰도 상승 |

---

## 5. 호출 예시

```bash
curl -X POST http://localhost:8000/recommend \
  -H "Content-Type: application/json" \
  -d @analysis_result.json
```

```js
const res = await fetch("http://localhost:8000/recommend", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ cnn_result, llm_result, survey, budget_total })
});
const routine = await res.json();
```

---

## 6. 기타 엔드포인트
- `GET /health` — 서버 상태
- `GET /meta` — 클래스 목록, 설문 코드표. **모델팀 `class_to_idx`와 대조용**

## 7. 파일 구성
| 파일 | 역할 | 수정? |
|---|---|---|
| `main.py` | API 진입점 + 입력 검증 | X |
| `survey_map.py` | 설문 → 가중치 매핑 | 튜닝 시 |
| `ml_engine.py` | 랭킹·제약·슬롯 조립 | X |
| `package_rules.py` | 진단별 프로파일 | 튜닝 시 |
| `적합도_전상품.csv` | 모델 예측 5,523개 | X |
| `상품별_효능_v4.csv` | 성분 효능 데이터 | X |

**폴더 통째로 받아서 실행만 하면 됩니다.**
