# 백엔드 연동 가이드

> AI 파트는 **HTTP API 2개**를 제공합니다. 백엔드는 순서대로 호출만 하면 됩니다.
> 모델 파일이나 파이썬을 직접 다룰 필요 없습니다.

---

## 1. 전체 흐름

```
[앱] 사진 + 설문
      │
      ▼
[백엔드] ──①──▶ 피부 분석 API :8001  POST /analyze
      │◀── { cnn_result, llm_result, survey } ──┘
      │
      ├──②──▶ 추천 API :8000  POST /recommend
      │◀── { 헤드라인, 구성:[토너,로션,크림], 총액 } ──┘
      ▼
    [앱]
```

**①의 응답을 그대로 ②에 넘기면 됩니다.** 변환 코드 불필요.
순차 호출입니다(①의 결과가 ②의 입력). 병렬 불가.

---

## 2. 실행

```bash
docker compose up -d
```

| 서비스 | 포트 | 내용 |
|---|---|---|
| `skin-analysis` | 8001 | CNN + LLM 진단 |
| `recommend-api` | 8000 | 화장품 추천 |

`model/.env`에 LLM API 키가 필요합니다 (`model/.env.example` 참고).

Docker 없이:
```bash
cd model && uvicorn server:app --port 8001 &
uvicorn main:app --port 8000 &
```

Swagger UI: `localhost:8001/docs`, `localhost:8000/docs`

---

## 3. ① 피부 분석 API

### `POST /analyze`  (multipart/form-data)

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `image` | file | O | 얼굴 사진 (jpeg/png/webp, 최대 10MB) |
| `answers` | string | X | 설문 응답 JSON. **한글 라벨 그대로** |
| `top_k` | int | X | 기본 3 |

```js
const form = new FormData();
form.append("image", photoFile);
form.append("answers", JSON.stringify({
  skin_type: "지성",
  main_concern: ["여드름/뾰루지", "블랙헤드/모공"],
  duration: "3개월 이상",
  location: ["턱", "볼"],
  sensitivity: "가끔 그렇다",
  history: "없음"
}));

const analysis = await fetch("http://skin-analysis:8001/analyze", {
  method: "POST", body: form
}).then(r => r.json());
```

**응답**
```json
{
  "cnn_result": {
    "predicted_label": "acne_rosacea",
    "confidence": 0.9891,
    "top_k": [{"label":"acne_rosacea","confidence":0.9891}, ...]
  },
  "llm_result": {
    "summary": "...",
    "likely_conditions": ["여드름(Acne)", "주사 피부염(Rosacea)"],
    "care_recommendations": ["...", "..."],
    "need_professional_care": true,
    "disclaimer": "..."
  },
  "survey": { "skin_type": 2, "concerns": [1,2], "duration": 5 }
}
```

### `GET /survey`
설문 문항 스키마. **프론트가 화면 그릴 때 사용하세요.**
문항 텍스트를 하드코딩하면 나중에 문항이 바뀔 때 어긋납니다.

### `GET /health`
`model_loaded`로 모델 로딩 여부 확인.

---

## 4. ② 추천 API

### `POST /recommend`  (application/json)

①의 응답 **그대로** + 선택 옵션

```js
const routine = await fetch("http://recommend-api:8000/recommend", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ ...analysis, budget_total: 90000 })
}).then(r => r.json());
```

| 추가 필드 | 필수 | 설명 |
|---|---|---|
| `budget_total` | X | 3개 합계 예산(원) |
| `alpha` | X | ML 비중 0~1. 기본 0.6 (건드리지 않아도 됨) |

**응답**
```json
{
  "진단": "acne_rosacea",
  "헤드라인": "당신은 여드름·주사성 피부입니다",
  "패키지명": "여드름·주사성 피부 패키지",
  "요약": "피지·모공 관리와 진정을 함께. 유분 많은 제형은 피하세요.",
  "신뢰도": 0.989,
  "트리아지": "미용케어",
  "의료상담권고": true,
  "의료권고사유": ["AI 분석 소견", "증상 3개월 이상 지속"],
  "반영된설문": { "피부타입": "지성", "고민": ["여드름/뾰루지"], "무향필터": "선호" },
  "분석요약": "(llm_result.summary 그대로)",
  "생활수칙": ["(llm_result.care_recommendations 그대로)"],
  "고지사항": "...",
  "총액": 50100,
  "구성": [
    {
      "순서": 1,
      "슬롯": "토너",
      "goods_no": "A000000113008",
      "brand": "셀린저",
      "name": "셀린저 리얼 토너 씨벅톤 200ml",
      "가격": 10000,
      "적합도": { "지성적합": 33.0, "저자극": 70.0, "진정효과": 53.0 },
      "적합도출처": "실측",
      "고시": "미백:나이아신아마이드",
      "무향": true,
      "코메도": 0
    }
  ]
}
```

---

## 5. 화면 매핑

```
┌──────────────────────────────┐
│ 진단 결과                      │ 고정 텍스트
│ 당신은 여드름·주사성 피부입니다   │ 헤드라인
│ 피지·모공 관리와 진정을 함께...   │ 요약
│                              │
│ ┌ 여드름·주사성 피부 패키지 ───┐ │ 패키지명
│ │ [img] 토너                 │ │ 구성[0].슬롯
│ │       셀린저 리얼 토너 씨벅톤 │ │ 구성[0].name
│ │       10,000원             │ │ 구성[0].가격
│ │ [img] 로션                 │ │
│ │ [img] 논코메도제닉 수분젤     │ │ 진단 따라 이름 바뀜
│ └────────────────────────────┘ │
│  총 50,100원                   │ 총액
│ [장바구니] [구매하러 가기]        │
└──────────────────────────────┘
```

**`순서`가 바르는 순서입니다** (토너 → 로션 → 크림). 점도 순이라 고정입니다.

**구매 링크**
```
https://www.oliveyoung.co.kr/store/goods/getGoodsDetail.do?goodsNo={goods_no}
```

**추천 이유 표시**
`적합도`를 그대로 쓰면 됩니다. `적합도출처`가 `실측`이면 실제 리뷰 수천 건 기반이라
"리뷰 기반" 배지를 붙이면 신뢰도가 올라갑니다.
현재 5,523개 중 4,277개(77%)가 실측입니다.

---

## 6. 반드시 처리할 분기

```js
// 1) 의료 권고 — 가장 중요
if (routine.의료상담권고) {
  // 제품 카드보다 병원 안내 UI를 먼저 노출
  // routine.의료권고사유 → ["AI 분석 소견", "진단 이력(아토피피부염)"]
}

// 2) 낮은 신뢰도
if (routine.신뢰도 < 0.5) {
  // "참고용" 배지 표시
}

// 3) 고지사항
// routine.고지사항을 화면 하단에 항상 노출
```

아토피·건선·진균감염은 **화장품으로 해결되지 않습니다.** 제품부터 보여주면 치료가 늦어질 수 있습니다.

---

## 7. 에러 처리

### ① 피부 분석
| 코드 | 원인 |
|---|---|
| 400 | 지원하지 않는 이미지 형식 / `answers` JSON 파싱 실패 / 설문 값 오류 |
| 413 | 이미지 10MB 초과 |
| 500 | 모델 추론 실패 (로그 확인) |

### ② 추천
| 코드 | 메시지 | 원인 |
|---|---|---|
| 400 | `확률 합이 1이 아님` | 진단 API가 softmax가 아닌 값을 보냄 |
| 400 | `알 수 없는 클래스` | 클래스명 오타 |
| 400 | `피부타입은 [...] 중 하나` | 설문 코드 매핑 오류 |

400이 뜨면 **진단 API 출력을 먼저 점검**하세요.

---

## 8. 성능 / 타임아웃

| 단계 | 소요 |
|---|---|
| CNN 추론 | 50~200ms |
| LLM 분석 | 1~3초 |
| 추천 | ~100ms |
| **합계** | **3~10초** |

**백엔드 타임아웃 30초 권장.**

첫 요청은 모델 로딩으로 **15초까지** 걸립니다.
서버 기동 후 `/health` 또는 더미 `/analyze`로 워밍업하세요.

---

## 9. DB 저장 권장

구독형 서비스라 결과를 남겨야 합니다. **응답 전체를 JSON으로 통째로** 저장하는 게 편합니다.

```sql
CREATE TABLE analysis_result (
  id            BIGSERIAL PRIMARY KEY,
  user_id       BIGINT NOT NULL,
  image_url     TEXT,
  diagnosis     VARCHAR(64),      -- "acne_rosacea"
  confidence    REAL,             -- 0.9891
  needs_medical BOOLEAN,          -- 의료상담권고
  total_price   INT,              -- 총액
  raw_response  JSONB,            -- 전체 응답
  created_at    TIMESTAMPTZ DEFAULT now()
);
```

정규화하면 스키마가 자주 바뀝니다. `raw_response`에 넣고 자주 조회하는 것만 컬럼으로 빼세요.

---

## 10. 참고

- 클래스 순서 대조: `GET /meta` (추천 API) ↔ `model/checkpoints/class_names.json`
  어긋나면 **에러 없이 조용히 잘못된 추천**이 나갑니다. 배포 전 한 번 확인하세요.
- 데이터 갱신: `updater/README.md` 참고 (신제품·리뷰 반영)
